{% extends 'base.html.twig' %}

{% block title %}Book index{% endblock %}

{% block body %}
    <h1>List of all books:</h1>
    <div class="form-row align-items-center ml-3">  
    {{include('includes/_sort.html.twig')}}  
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Author</th>
                <th>Year</th>
                <th>ISBN</th>
                <th>FrontPage</th>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for book in books %}
            <tr>
                <td>{{ book.title }}</td>
                <td>{{ book.author }}</td>
                <td>{{ book.year }}</td>
                <td>{{ book.ISBN }}</td>
                <td>  <img src="{{ asset('uploads/frontpages/' ~ book.frontpage) }}"alt="" border=3 height=100 width=100></img>  </td>
                <td>{{ book.id }}</td>
                <td>
                    <a href="{{ path('book_show', {'id': book.id}) }}">show</a>
                    <a href="{{ path('book_edit', {'id': book.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div>
        {{ knp_pagination_render(books) }}
    </div>
    <div>
        Total books: {{ books.getTotalItemCount }}
    </div>

    <a href="{{ path('book_new') }}">Create new</a>
{% endblock %}
